/**
 *  Create new project on firebase!!
 * Install firebase
 * Create firebase.init.js and export auth.
 * Go to firebase console > Authentication > Enable sign in method.
 **/

/**
 * Hosting Steps (commands):
 * npm install -g firebase-tools (first time)
 * firebase login (one time)
 * firebase init (for each project one time)
 * npm run build (Every time we want to deploy)
 * firebase deploy (Every time we want to deploy) 
*/